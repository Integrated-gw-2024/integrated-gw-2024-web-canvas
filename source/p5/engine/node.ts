/*
export abstract class Node {
  protected parentNode: Node | null;

  constructor() {
    this.parentNode = null;
  }

  public remove(node: Node): void {
    if (node.parentNode == null) {
      throw new Error(`parentNode of '${node}' is null.`);
    }

    node.parentNode.remove(node);
  };
}
*/
